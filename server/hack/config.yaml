# gf CLI 仅开发机使用。不要把账号密码提交进来。
# https://goframe.org/docs/cli
gfcli:
  docker:
    build: "-a amd64 -s linux -p temp -ew"
  # gf CLI 默认不含 sqlite 驱动（需自行编译）。本仓用 gdb.New + internal/model/do，暂不 gf gen dao。
  # gen:
  #   dao:
  #     - link: "sqlite::@file(data/fenghuolun.db)"
